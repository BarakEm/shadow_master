<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ShadowMaster" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/primary</item>
    </style>

    <!-- Splash screen theme for AndroidX SplashScreen API -->
    <style name="Theme.ShadowMaster.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.ShadowMaster</item>
    </style>
</resources>
