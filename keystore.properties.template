# Keystore Properties Template
# Copy this file to keystore.properties and fill in your actual values
# The keystore.properties file is gitignored and will not be committed

# Path to your keystore file (can be absolute or relative to project root)
storeFile=path/to/your/keystore.jks

# Keystore password
storePassword=your_keystore_password

# Key alias
keyAlias=your_key_alias

# Key password
keyPassword=your_key_password
